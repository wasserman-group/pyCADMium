name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - wget
  - setuptools
  - python
  - pip
  - numpy
  - scipy 
  - libxc 

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

