name: test
channels:
  - conda-forge
  - anaconda
dependencies:
    # Base depends
  - wget
  - setuptools
  - python
  - pip
  - numpy
  - scipy 
  - libxc 
  - pydantic

    # Testing
  - pytest
  - pytest-cov
  - codecov

  #Libxc
  - cmake

    # Pip-only installs
  - pip:
   - codecov
   - pylibxc2

